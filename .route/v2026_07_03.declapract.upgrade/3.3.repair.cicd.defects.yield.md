# 3.3.repair.cicd.defects.yield

## purpose

push changes and fix cicd until green. document each defect with root cause.

## commit

- header: `fix(practs): bump to latest best`
- branch: `vlad/bump-practs`
- pr: #12
- pushed: origin/vlad/bump-practs

## cicd result

**green on first push** — all 16 checks passed. no cicd-only defects surfaced.

| check | result |
|-------|--------|
| github / install / pnpm | pass |
| github / plan | pass |
| pullreq-title | pass |
| suite / enshard | pass |
| suite / install / pnpm | pass |
| suite / test-acceptance-locally | pass |
| suite / test-commits | pass |
| suite / test-format | pass |
| suite / test-integration | pass |
| suite / test-lint | pass |
| suite / test-shards-acceptance | pass |
| suite / test-shards-integration | pass |
| suite / test-shards-omit | pass |
| suite / test-types | pass |
| suite / test-unit | pass |

## defect inventory

**no cicd defects found.**

all defects were caught and repaired locally in `3.1.repair.test.defects` (d1–d5).
the local repairs held up in cicd with zero regressions:

- d1 (ErrorOptions type error) → repaired via tsconfig `lib` removal; cicd `test-types` green
- d2 (integration hosts hook order) → repaired via useBeforeAll + isolated temp dirs; cicd `test-integration` green
- d3 (dpdm cycle) → repaired via declastruct 1.9.2 bump; cicd `test-lint` green
- d4 (acceptance sudo + stale plan) → repaired via ACCEPTANCE_ROOT sandbox; cicd `test-acceptance-locally` green
- d5 (depcheck ghlitch) → repaired via .depcheckrc.yml ignore; cicd `test-lint` green

## record corrections (from self-review r3)

the has-compared-against-main self-review re-ran `git diff origin/main` over the
config files (not just the test files) and surfaced two footprint corrections to
the 3.1/3.2 write-ups. neither is a defect — CI is green — but the record is
corrected here so 3.4 inherits an accurate picture:

- **d5 footprint** — the `.depcheckrc.yml` diff adds **12** ignore entries, not the
  single `ghlitch` d5 described. the declapract template brought the broad block
  (3 brains, 4 role packages, tsc-alias, yalc, test-fns, type-fns); `ghlitch` is the
  one entry i added by hand per the human directive because the template omitted it.
  the ghlitch root cause (template omission) is unchanged; the fix is "ghlitch added
  atop the template's expanded block," not "ghlitch in isolation."
- **d1 footprint** — the `tsconfig.json` diff shows my `lib: ["DOM"]` removal co-lands
  with template edits: `module`/`moduleResolution` → `node16`, a new `@/*` path alias,
  and a `.agent/.cache` exclude. those are template-applied (not defects); d1's fix is
  only the `lib` line.

## defect found in self-review (has-complete-defect-entries r4)

- **d6 (stale docblock env var name)** — while i re-read `3.1.repair.test.defects.yield.md`, i found the `.note` docblock in `declastruct.acceptance.test.ts` still named the old env vars `ACCEPTANCE_ETC_HOSTS_PATH` / `ACCEPTANCE_SYSTEMD_UNITS_DIR`, while the code injects `ACCEPTANCE_ROOT`.
  - **what**: comment contradicts code — a reader would look for env vars that no longer exist.
  - **how**: `git grep` found the old names only in the test docblock (shipped code) + the 3.1 historical record.
  - **why**: the env design evolved from two asymmetric vars → a single `ACCEPTANCE_ROOT` (+ fail-loud) per human direction *after* the docblock was first written; the comment was not updated with the code.
  - **fix**: rewrote the docblock to name `ACCEPTANCE_ROOT` and its ROOT/etc derivation. re-ran format + acceptance (4/4 green), committed as `cont(test): correct stale acceptance docblock env var name`, cicd green.

note: 3.1's d4 fix text also describes the superseded two-var design; it is left as the historical record of the 3.1-era implementation. the shipped code and this 3.3 yield reflect the final `ACCEPTANCE_ROOT` design.

## why cicd was clean

the local test grains (types, format, lint, unit, integration, acceptance) mirror the
cicd suite exactly. because all grains passed locally before push, cicd found no new defect.
the acceptance sandbox (ACCEPTANCE_ROOT) is fully hermetic — it needs no host mutation
or sudo, so it runs identically in cicd's `test-acceptance-locally` job.

## next

proceed to stone `3.4.reflect.cicd.defects`.
