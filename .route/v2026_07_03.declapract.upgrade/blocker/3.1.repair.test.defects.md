# blocker: 3.1.repair.test.defects

## what blocks me

the **acceptance** grain cannot fully run in this local worktree.

`rhx git.repo.test --what acceptance --against local --env test --mode apply` → `2 passed, 2 failed`.

the 2 failures are the `declastruct apply` tests, which mutate the **real** `/etc/hosts` via `sudo`:

```
Command failed: sudo tee -a /etc/hosts
stderr: sudo: a terminal is required to read the password;
        either use the -S option ... or configure an askpass helper
        sudo: a password is required
```

jest runs non-interactively and this shell has no passwordless sudo, so the privileged write to `/etc/hosts` cannot proceed. this is an **absent-resource / privilege** condition, not a code or upgrade defect (the plan-generation tests, which need no privilege, pass; origin/main's same test would need the same privilege).

## what i tried

- **all other grains green**:
  - `test:types` → pass (fixed d1: removed stale `lib: ["DOM"]` so `ErrorOptions` resolves)
  - `test:unit --thorough` → 39/39 pass
  - `test:integration --thorough` → 16/16 pass (fixed d2: hosts-file test isolation via `useBeforeAll` + per-case temp dirs)
  - `test:lint:biome` → pass
  - `test:lint:cycles` (dpdm) → pass (fixed the cycle by `declastruct` 1.9.1 → 1.9.2, which drops the cycle-prone transitive `helpful-errors@1.5.3`)
- **acceptance plan path** (2 tests, no privilege) → pass
- **acceptance apply path** (2 tests) → fail solely on `sudo: a password is required`
- confirmed the failure is environmental: the command is `sudo tee -a /etc/hosts`; there is no way for me to supply a sudo password non-interactively, and i should not modify sudoers or the real `/etc/hosts` without human consent

## what i need

any one of these would unblock the acceptance apply path:

1. **run in an environment with passwordless sudo** (CI acceptance runners typically have this), or
2. **cache sudo credentials first**, then re-run: run `sudo -v` in this shell, then
   `rhx git.repo.test --what acceptance --against local --env test --mode apply`, or
3. **you run the acceptance command directly** here via the `!` prefix:
   `! rhx git.repo.test --what acceptance --against local --env test --mode apply`

please advise which path you'd like, or confirm whether acceptance-apply is expected to run only in CI (in which case the local run is not a real gate and i can proceed).
