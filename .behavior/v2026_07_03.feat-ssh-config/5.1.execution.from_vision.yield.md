# 5.1 execution — from vision

> build the two declared resources from the approved vision:
> `DeclaredUnixSshKeypair` + `DeclaredUnixSshAlias`, symmetric with the extant
> hostAlias / portAlias resources.

## todos

### shared
- [x] lift `UnixPortEndpoint` → shared `UnixEndpoint` domain object; reuse in portAlias

### resource 1 — DeclaredUnixSshKeypair
- [x] `domain.objects/DeclaredUnixSshKeypair.ts`
- [x] sdk `access/sdks/osUnixSshKeygen/` (dobj, cast, get/set/del dops, index) — shells out to ssh-keygen, reads `.pub`
- [x] operations `domain.operations/sshKeypair/` (getOne, getAll, set, del, casts)
- [x] dao `access/daos/DeclaredUnixSshKeypairDao.ts`
- [x] tests (unit casts + integration sdk/ops)

### resource 2 — DeclaredUnixSshAlias
- [x] `domain.objects/DeclaredUnixSshAlias.ts`
- [x] sdk `access/sdks/osUnixSshConfig/` (dobj, cast, get/add/replace/del dops, index) — parse `Host` blocks
- [x] operations `domain.operations/sshAlias/` (getOne, getAll, set, del, casts)
- [x] dao `access/daos/DeclaredUnixSshAliasDao.ts`
- [x] tests (unit casts + integration sdk/ops)

### provider + exports
- [x] `ContextUnixNetwork` — add `sshConfigPath` (+ keys dir for acceptance)
- [x] `getDeclastructUnixNetworkProvider` — register both daos + defaults
- [x] `DeclastructUnixNetworkProvider` type — add both dao types
- [x] `contract/sdks/index.ts` — export new dobjs + operations
- [x] acceptance asset — add keypair + ssh alias resources

### verify
- [x] `rhx git.repo.test --what types`
- [x] `rhx git.repo.test --what lint`
- [x] `rhx git.repo.test --what format`
- [x] `rhx git.repo.test --what unit --mode apply` — 34 passed
- [x] `rhx git.repo.test --what integration --mode apply` — 54 passed
- [x] `rhx git.repo.test --what acceptance --against local --env test --mode apply` — 4 passed

## notes — declastruct integration fixes (found at acceptance)

1. `DeclaredUnixSshAlias.key` (a `RefByUnique`) must be declared in `static nested`
   so declastruct's serializer can manipulate it. mirrors
   `DeclaredAwsEc2InstanceSession.instance`.
2. `DeclaredUnixSshKeypair.publicKey` must be declared `static readonly` so
   declastruct excludes the mint-resolved field from its plan diff. absent that,
   a re-plan reads `publicKey` (set after mint) vs declared (undefined) as a
   change and tries to UPDATE the findsert-only keypair dao. mirrors
   `DeclaredAwsEc2Instance.privateIp`.
