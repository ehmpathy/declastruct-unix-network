# blocker: 5.1.execution.from_vision

## what blocks me

the stone cannot pass its peer-review guard, but not because of a real code
defect. the reviewer system itself is non-converging:

- **l1 reviewers r2–r9 are budget-exhausted (3/3).** on the latest re-review
  (i4) they no longer run — they serve `cached` verdicts from i3. so the fixes
  made since i3 cannot be re-judged by them. their stale blockers are frozen.
- **l1 r1 (repo-rules) is an infra misconfig.** its rules glob
  `.agent/repo=.this/**/rule.*.md` matches no files in this repo, so it errors
  as a "constraint" rather than reviewing any code.
- **l3 r10 (enroll-impl-behavior-intent) malfunctioned** — timed out after 21
  minutes. no verdict rendered.
- **l3 r11 (enroll-impl-arch-defects) is on the same 21-min timeout path.**

the cached l1 blockers are all phantom:
- r4/r6 `del`-verb "forbidden" — false; `del` is the authoritative declastruct
  convention (declastruct-aws uses del* for ~25 ops). rule clarification
  dispatched as ehmpathy/rhachet-roles-ehmpathy#492.
- r3 map/filter decode-friction — already fixed (ejected into
  `castIntoDeclaredUnixSshAliases` transformer + unit tests).
- r7 race conditions + di-of-execa — human already judged these overkill.
- r8/r9 snapshot coverage — already added ("use both": snapshots alongside the
  extant assertions on all 4 ssh integration tests).

## what i tried

- fixed all three human-directed items (del radio task #492, map/filter
  ejection + tests, snapshot coverage).
- verified every objective gate is green:
  - types ✓, format ✓, lint ✓
  - unit 54/54 ✓
  - integration 61/61 ✓
  - acceptance 4/4 ✓
- re-signaled `--as arrived` to re-run peer review (i4). result: l1 exhausted/
  cached, l1 r1 misconfig, l3 r10 timeout crash, l3 r11 pending.

## what i need

a human decision, because `--as approved` / `--as overruled` is human-only and
i will not self-approve:

- **overrule** the exhausted/crashing reviewers on the strength of the green
  gates and the phantom-only blockers, via
  `rhx route.stone.set --stone 5.1.execution.from_vision --as overruled`; or
- direct me to try something specific if a real concern remains.

re-running review again is not productive — it only re-runs exhausted l1s and
re-crashes the timing-out l3s for 20+ minutes with no new signal.
